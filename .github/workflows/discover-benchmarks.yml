name: Discover new benchmarks

on:
  schedule:
    - cron: "0 15 1 * *"  # First of every month at 11am ET (15:00 UTC)
  workflow_dispatch:       # Allow manual trigger from GitHub Actions tab

jobs:
  discover:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Research new benchmarks
        run: |
          cd $GITHUB_WORKSPACE
          topics=(
            "benchmarks that test whether AI can identify the news story or key finding in a messy dataset or CSV file"
            "benchmarks that test whether AI can verify a specific claim or statistic against an authoritative source such as a government database"
            "benchmarks that test whether AI accurately conveys uncertainty, confidence, or limitations in a way a non-expert can act on"
            "benchmarks that test end-to-end data analysis: loading data, reasoning over it, and producing an accurate narrative or summary"
          )
          for topic in "${topics[@]}"; do
            echo "Researching: $topic"
            python3 scripts/research_agent.py research "$topic" || echo "Failed: $topic"
            sleep 5
          done

      - name: Commit draft entries to research_inbox
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add research_inbox/
          if git diff --cached --quiet; then
            echo "No new drafts generated."
          else
            count=$(git diff --cached --name-only | grep -c "research_inbox/" || true)
            git commit -m "chore: add $count benchmark draft(s) for review $(date -u +%Y-%m-%d)"
            git push
          fi
